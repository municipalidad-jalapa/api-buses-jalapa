package gt.muni.jalapa.ecoruta.tiempo.servicio;

/** Parada reducida al orden y las coordenadas que usa el calculo (HU-72). */
public record PuntoDeParada(int orden, double latitud, double longitud) {
}
